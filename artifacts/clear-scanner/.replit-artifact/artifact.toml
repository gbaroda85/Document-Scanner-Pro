kind = "web"
previewPath = "/"
title = "Clear Scanner"
version = "1.0.0"
id = "artifacts/clear-scanner"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24515

[services.development]
run = "pnpm --filter @workspace/clear-scanner run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/clear-scanner", "run", "build" ]
publicDir = "artifacts/clear-scanner/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24515"
BASE_PATH = "/"
